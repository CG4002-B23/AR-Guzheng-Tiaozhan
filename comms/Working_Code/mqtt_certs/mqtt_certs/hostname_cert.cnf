[ req ]
default_bits = 2048
prompt = no
default_md = sha256
distinguished_name = dn
req_extensions = v3_req

[ dn ]
CN = mqtt-broker.local

[ v3_req ]
subjectAltName = @alt_names

[ alt_names ]
DNS.1 = mqtt-broker.local
DNS.2 = localhost
IP.1 = 127.0.0.1
